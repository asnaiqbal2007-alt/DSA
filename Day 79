#include <stdio.h>
#include <limits.h>

#define MAXN 1005

int main() {
    int n, m;
    scanf("%d %d", &n, &m);
    
    // Adjacency list: graph[u][v] = weight
    int graph[MAXN][MAXN];
    for(int i = 1; i <= n; i++) {
        for(int j = 1; j <= n; j++) {
            graph[i][j] = INT_MAX;
        }
        graph[i][i] = 0;
    }
    
    // Read edges
    for(int i = 0; i < m; i++) {
        int u, v, w;
        scanf("%d %d %d", &u, &v, &w);
        graph[u][v] = w;
    }
    
    int source;
    scanf("%d", &source);
    
    // 

